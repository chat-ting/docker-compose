spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/chat_ting
    username: postgres
    password: qwer1234
    driver-class-name: org.postgresql.Driver
  data:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace
auth:
  token-ttl-minutes: 60